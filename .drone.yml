pipeline:  

  publish:
    image: plugins/docker
    repo: lobre/wordpress-ci
    secrets: [ docker_username, docker_password ]
  deploy:
    image: appleboy/drone-ssh     
    host: lobr.fr     
    username: $${DEPLOY_USERNAME}     
    password: $${DEPLOY_PASSWORD}
    secrets: [ deploy_username, deploy_password ]
    port: 22
    script:
      - echo hello       
      - echo world  
